[package]
name = "garage_rfid"
version = "0.1.0"
authors = ["olson3r <scootles.olson@gmail.com>"]

[features]
default = []
# default = ["mio-evented"]
# mio-evented = ["sysfs_gpio/mio-evented"]
# tokio = ["sysfs_gpio/tokio"]

[dependencies]
config = "0.8"
error-chain = "0.11.0"
diesel = { version = "1.1.1", features = ["chrono", "sqlite", "uuid"] }
mio = "0.6"
serde = "1.0.27"
serde_derive = "1.0.27"
sysfs_gpio = { version = "0.5", features = ["mio-evented"] }
uuid = "0.6.1"

[dev-dependencies]
dotenv = "0.10.1"
